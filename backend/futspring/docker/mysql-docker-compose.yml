version: '3.1'

services:
  db:
    image: mysql:8.0
    volumes:
      - dbbase:/var/lib/mysql
    ports:
      - "3309:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 123
      MYSQL_DATABASE: futspring_db

  adminer:
    image: adminer
    ports:
      - 8000:8080

volumes:
  dbbase: